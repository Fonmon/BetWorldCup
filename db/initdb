#!/bin/bash

pwd=$1
docker exec -i betworldcup_db mysql -uworldcupuser -p${pwd} betworldcup < init.sql
./../utils/generate_inserts.py | docker exec -i betworldcup_db mysql -uworldcupuser -p${pwd} betworldcup